# Claude Code

Read and apply `AGENTS.md` before making any change.

Tool-independent roles are defined in `.agents/roles/`. Files in `.claude/agents/` are invocation
adapters only and must not duplicate repository rules.
